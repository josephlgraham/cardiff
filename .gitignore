.claude/
node_modules/

# High-res image originals. The site only serves the optimized WebP under
# assets/ and media/. Keep your originals here (and backed up elsewhere) —
# `npm run generate-assets` reads from _src/ and writes the served files.
_src/
